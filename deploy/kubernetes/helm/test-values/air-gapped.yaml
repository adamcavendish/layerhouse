storage:
  s3:
    endpoint: http://rustfs.rustfs.svc.cluster.local:9000
    bucket: layerhouse
    existingSecret: layerhouse-s3

server:
  tls:
    existingSecret: layerhouse-server-tls
    dnsNames:
      - registry.airgap.local

raft:
  tls:
    existingSecret: layerhouse-raft-mtls
